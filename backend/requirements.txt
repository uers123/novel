flask>=3.0
flask-cors>=4.0
requests>=2.28
beautifulsoup4>=4.11

# Optional runtime adapters:
# MeloTTS provides backend local TTS. Install it when local voice synthesis is needed.
# llama-cpp-python provides nocle GGUF translation. Install the CUDA/CPU wheel that matches the machine.
# MeloTTS
# llama-cpp-python
